
# these are bash commands
source ~/.bashrc
mamba create -n RLDNe_env 
mamba install -c conda-forge   r-base=4.2 r-readxl r-dplyr
mamba activate RLDNe_env
R

#  now you are in the R environment. I had problems to install rldne with mamba, thats why I used: 

if(!require("RLDNe")) {
  # install devtools if it does not already exist
  if(!require("devtools")) {
    install.packages("devtools", repos = "https://cloud.r-project.org")
    library(devtools)
  }
  # install RLDNe from GitHub including all dependencies
  devtools::install_github("zakrobinson/RLDNe", dependencies = TRUE)
}